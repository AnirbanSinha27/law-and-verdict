declare module "hast" {
  // Minimal placeholder typings for packages that expect the `hast` types.
  // Extend as needed when interacting directly with the AST.
  export type Root = unknown;
  export type Element = unknown;
  export type Text = unknown;
  export type Parent = unknown;
}

